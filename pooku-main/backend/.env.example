# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# Comma-separated allowed origins (default: http://localhost:5000)
CORS_ORIGINS=http://localhost:5000

# Server port
PORT=5000
